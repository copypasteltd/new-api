# File path: /etc/systemd/system/cvai.service
# sudo systemctl daemon-reload
# sudo systemctl start cvai
# sudo systemctl enable cvai
# sudo systemctl status cvai
[Unit]
Description=CVAI Service
After=network.target

[Service]
User=ubuntu  # 注意修改用户名
WorkingDirectory=/path/to/cvai  # 注意修改路径
ExecStart=/path/to/cvai/cvai --port 3000 --log-dir /path/to/cvai/logs  # 注意修改路径和端口号
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
